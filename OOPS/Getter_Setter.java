package OOPS;

public class Getter_Setter {
    public static void main(String[] args) {
        // Teachers t = new Teachers();
        // t.name = "raghav";
        //  // t.salary  --> not possible because it is private can't be acces outside o f class

        //  // but we access that function where salary [which is private btw] is returned.so that function is known as getter
        //  System.out.println(t.getId());  
         
        //  t.setId(72000);  // so even though salary is private but with the help[ of setter we are able to change that value/.
    }
}
